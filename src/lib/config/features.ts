/**
 * Trip dashboard games (lobby, caption-this, bingo, etc.).
 * Set to `true` to show nav, hero counts, onboarding copy, and `/trips/.../games` routes again.
 */
export const FEATURE_TRIP_GAMES = false;
